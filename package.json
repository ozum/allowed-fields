{
  "name": "allowed-fields",
  "version": "0.3.2",
  "description": "Define white listed and black listed database fields and check whether given field is allowed.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "bugs": {
    "url": "https://github.com/ozum/allowed-fields/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ozum/allowed-fields.git"
  },
  "files": ["lib"],
  "keywords": ["db fields", "whitelist", "blacklist"],
  "author": {
    "name": "Özüm Eldoğan"
  },
  "license": "MIT",
  "scripts": {
    "watch": "concurrently 'moe-scripts build --watch > /dev/null' 'moe-scripts test --watch'",
    "module": "npm install ozum/moe-scripts#master -save-dev",
    "build": "moe-scripts build --watch",
    "build:doc": "moe-scripts doc --no-cache",
    "test": "moe-scripts test",
    "test:update": "moe-scripts test --updateSnapshot",
    "lint": "moe-scripts lint",
    "format": "moe-scripts format",
    "validate": "moe-scripts validate",
    "postversion": "git push && git push --tags && npm publish",
    "prepublishOnly": "npm run build",
    "nodemon": "nodemon --delay 0.2 -r source-map-support/register",
    "watch:build": "moe-scripts build --watch",
    "watch:test": "moe-scripts test --watch"
  },
  "devDependencies": {
    "moe-scripts": "0.0.6"
  },
  "dependencies": {
    "@types/joi": "^13.0.5",
    "joi": "^13.1.2"
  },
  "readme": "ERROR: No README data found!",
  "homepage": "https://github.com/ozum/allowed-fields#readme",
  "_id": "allowed-fields@0.3.2"
}
